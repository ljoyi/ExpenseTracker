import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationStack {
            Color(uiColor: .systemGroupedBackground)
                .ignoresSafeArea()
        }
    }
}
