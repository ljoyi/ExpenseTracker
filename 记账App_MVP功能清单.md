# 记账 App MVP 功能清单

> 本文是 MVP 总范围清单。P0 的正式冻结口径、页面、数据模型、Service 和任务拆分以 [`docs/README.md`](docs/README.md) 为准；若两份文档出现冲突，以 `docs` 目录内的 P0 文档为准。

## 1. 产品定位

一个面向个人用户的 iOS 记账工具，核心价值是：

1. 用尽量少的步骤完成一笔收支记录。
2. 能按日期回看每一笔账单。
3. 能快速看清某个周期内钱花到了哪里。

MVP 只验证“记账是否顺手、回看是否清楚、统计是否有价值”这条核心闭环，不追求一次覆盖完整财务系统。

## 2. MVP 目标与边界

### 首发目标

- 默认进入账单页，用户无需登录即可开始记账。
- 用户可在 10 秒内完成一笔普通支出记录。
- 用户可按日期查看、修改和删除账单。
- 用户可查看本月及任意月份的收支概要、分类占比和每日趋势。
- 收入与支出使用统一的数据模型，统计准确。

### 本期不做

- 多用户、多人协作、账本成员
- 多账本切换
- 资产总览、账户余额联动
- 预算与超支提醒
- 银行账单导入、自动记账
- 云同步、跨设备同步、登录体系
- 定期账单、报销流程、复杂退款流程
- 多币种和汇率换算
- Widget、Apple Watch、快捷指令

## 3. 核心用户流程

```text
首次打开
  -> 创建默认账本
  -> 查看空账单页
  -> 点击“+”
  -> 选择支出/收入
  -> 输入金额
  -> 选择分类和日期时间
  -> 保存
  -> 返回账单页查看记录
  -> 切换日历日期查看当日明细
  -> 进入统计页查看月收入、支出、结余和分类占比
```

## 4. MVP 功能表单

字段说明：

- `P0`：不做则核心闭环不成立。
- `P1`：首版建议具备，用于提升可用性。
- `P2`：不进入 MVP，后续版本考虑。
- `验收标准`：首版可测试、可判断完成的功能边界。

| ID | 模块 | 功能项 | 优先级 | MVP范围 | 验收标准 | 版本 |
| --- | --- | --- | --- | --- | --- | --- |
| M01 | 启动与账本 | 默认账本 | P0 | 首次启动自动创建“我的账本” | 用户无需配置即可进入账单页并开始记账 | MVP |
| M02 | 导航 | 首页信息架构 | P0 | 首页为账单，提供“账单/统计/我的”底部导航 | 三个主入口可稳定切换，当前页面状态清晰 | MVP |
| M03 | 账单首页 | 月度概要 | P0 | 展示当前月份收入、支出、结余 | 数字与当月账单明细汇总一致，切月后同步更新 | MVP |
| M04 | 账单首页 | 月份切换 | P0 | 支持手动切换任意年月 | 切换月份后概要、日历和明细均使用同一月份 | MVP |
| M05 | 账单首页 | 日历视图 | P0 | 按日展示当日是否有收支，并支持选中日期 | 月份切换正确，选中日期高亮，跨月日期不串数据 | MVP |
| M06 | 账单首页 | 每日账单卡片 | P0 | 按日期分组展示收入、支出、结余和账单条目 | 无账单日期不显示空卡片，金额和分类显示正确 | MVP |
| M07 | 账单首页 | 账单详情 | P0 | 点击条目查看金额、分类、日期时间；备注不属于 P0 | 详情数据与录入数据一致 | MVP |
| M08 | 账单操作 | 修改账单 | P0 | 可修改类型、金额、分类、日期和时间；备注不属于 P0 | 修改后列表和统计立即更新，账本汇总不重复计算 | MVP |
| M09 | 账单操作 | 删除账单 | P0 | 删除前二次确认 | 删除后列表、统计和汇总同步更新 | MVP |
| M10 | 记账 | 新增入口 | P0 | 账单页固定“+”按钮打开记账面板 | 任意主页面均可快速进入记账流程 | MVP |
| M11 | 记账 | 收支类型 | P0 | 支持支出、收入；转账暂不进入 MVP | 切换类型后分类列表与保存数据正确 | MVP |
| M12 | 记账 | 金额输入 | P0 | 数字键盘、小数点、删除、保存；限制非法金额 | 金额大于 0 才能保存，最多保留两位小数 | MVP |
| M13 | 记账 | 分类选择 | P0 | 提供默认分类，选择后高亮 | 支出和收入分类相互独立，保存后分类正确 | MVP |
| M14 | 记账 | 日期与时间 | P0 | 默认当前时间，可手动修改 | 保存后的日期、时间和分组位置保持一致 | MVP |
| M15 | 记账 | 备注 | P1 | 支持输入可选文本备注 | 备注可保存、展示、修改；空备注不影响保存 | MVP |
| M16 | 记账 | 快捷连续记账 | P1 | 保存后保留当前类型和日期，可继续记下一笔 | 连续记账不需要重复设置相同字段 | MVP |
| M17 | 分类管理 | 默认分类 | P0 | 内置餐饮、交通、购物、居住、娱乐、医疗、工资等常用分类 | 新用户无需创建分类即可完成首笔记录 | MVP |
| M18 | 分类管理 | 新增分类 | P1 | 支持自定义名称、图标和颜色 | 新增分类立即可选，重启后仍存在 | MVP |
| M19 | 分类管理 | 修改分类 | P1 | 支持修改名称、图标和颜色 | 已关联账单显示更新后的分类信息 | MVP |
| M20 | 分类管理 | 删除分类 | P1 | 已被账单引用的分类不可直接删除，需先迁移或替换 | 不产生无分类账单，不丢失历史数据 | MVP |
| M21 | 统计 | 统计周期 | P0 | 默认本月，可切换上月、今年、去年、近一年和自定义日期 | 周期变化后以下所有统计使用相同时间范围 | MVP |
| M22 | 统计 | 收支总览 | P0 | 展示总收入、总支出、结余、日均支出 | 计算结果与筛选范围内的账单明细一致 | MVP |
| M23 | 统计 | 分类占比 | P0 | 用饼图展示支出/收入分类占比，并显示金额和比例 | 点击分类可查看该分类的账单明细 | MVP |
| M24 | 统计 | 趋势图 | P1 | 用柱状图或折线图展示按日/月的支出趋势 | 可切换支出、收入，图表与选定周期一致 | MVP |
| M25 | 统计 | 日/月报表 | P1 | 列表展示每日或每月的收入、支出、结余 | 点击报表行可进入对应日期或月份的账单明细 | MVP |
| M26 | 数据 | 本地持久化 | P0 | 账单、分类和设置保存在本机 | 杀掉 App 或重启设备后数据不丢失 | MVP |
| M27 | 数据 | 空状态与异常状态 | P1 | 首页、统计和分类页提供明确空状态 | 没有数据时不出现空白图表或错误数字 | MVP |
| M28 | 设置 | 基础设置 | P1 | 提供货币符号、周起始日、默认记账类型 | 设置变更在相关页面即时生效 | MVP |
| M29 | 数据安全 | 数据导出 | P2 | 导出 CSV/Excel 或备份文件 | 后续版本实现 | 后续 |
| M30 | 数据同步 | 登录与 iCloud 同步 | P2 | 多设备同步和恢复 | 后续版本实现 | 后续 |
| M31 | 资产 | 账户与余额 | P2 | 现金、银行卡、信用卡等账户及余额 | 后续版本实现 | 后续 |
| M32 | 预算 | 分类预算与提醒 | P2 | 设置月预算、查看使用进度 | 后续版本实现 | 后续 |
| M33 | 高级账单 | 转账与退款 | P2 | 账户间转账、部分退款与关联原账单 | 后续版本实现 | 后续 |
| M34 | 高级账单 | 图片附件 | P2 | 拍照或从相册添加凭证 | 后续版本实现 | 后续 |
| M35 | 多账本 | 账本切换与成员 | P2 | 多个账本及多人共享 | 后续版本实现 | 后续 |

## 5. MVP 页面清单

| 页面 | 核心内容 | 必须完成 |
| --- | --- | --- |
| 账单页 | 月份概要、月份切换、日历、按日账单列表 | 是 |
| 记一笔页 | 收支类型、分类、金额、日期时间、保存 | P0 是，备注为 P1 |
| 账单详情页 | 查看、修改、删除账单 | 是 |
| 统计页 | 周期筛选、收支总览、分类占比 | P0 是，趋势图和报表为 P1 |
| 分类管理页 | 查看、新增、修改、删除分类 | P1 |
| 设置页 | 基础偏好设置 | 否，可后置 |

## 6. MVP 验收指标

- 新用户首次启动后，可在 3 次点击内到达记账输入状态。
- 普通支出从打开记账页到保存完成，目标不超过 10 秒。
- 新增、修改、删除账单后，列表、汇总和统计结果同步且一致。
- 切换月份、日期和统计周期时，不出现串月、漏账或重复统计。
- App 冷启动无崩溃，离线可完成全部核心功能。
- 使用 1,000 条账单进行基本滑动、筛选和统计时保持可用。

## 7. P0 建议开发顺序

1. 数据模型、默认账本、内置分类和本地存储。
2. 记账闭环：新增、校验、保存和持久化。
3. 账单查看：月份概要、日历和每日账单列表。
4. 管理闭环：详情、修改和删除。
5. 洞察闭环：统计总览、分类占比和分类下钻。
6. 稳定性验证：状态、金额边界、日期边界、性能和数据一致性。

P1 再增加备注、连续记账、自定义分类、趋势图、日/月报表和基础设置。

## 8. 推荐技术基线

- 客户端：Swift + SwiftUI
- 本地数据：SwiftData；数据量或查询复杂度上升后可替换为 SQLite/GRDB
- 最低系统：iOS 17
- 图表：Swift Charts
- 架构：按功能拆分的轻量 MVVM，先以本地数据为唯一数据源
- 首发形态：iPhone 优先，暂不适配 iPad 和横屏

## 9. 首个迭代完成定义

当“新增一笔支出 -> 在账单页找到它 -> 修改金额 -> 删除它 -> 统计结果同步变化”这条链路完整可用，并且数据在重启后仍存在，即可认为 MVP 核心闭环成立。
